name: GitHub Actions
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: make build # отвечает за запуск gradle build
      - uses: docker://github/super-linter:v2.1.0 #отвечает за запуск линтера?